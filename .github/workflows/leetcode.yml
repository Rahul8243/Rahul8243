name: LeetCode Stats Update

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Update LeetCode Stats
        uses: joshcai/leetcode-stats-action@master
        with:
          username: rahul8243
